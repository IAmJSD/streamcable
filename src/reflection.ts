import type { ReadContext, Schema } from "./schemas";

export async function reflectToSchema(ctx: ReadContext): Promise<Schema<any>> {
    // TODO
    throw new Error("Reflection is not yet implemented!");
}
